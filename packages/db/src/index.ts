/**
 * @citadel/db
 *
 * Database client, Supabase integration, migrations, and RLS policies.
 * Single source of truth for all database access across the platform.
 */

export { createClient, type CitadelClient } from "./client.js";
